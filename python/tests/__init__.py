"""Test package marker.

Without this file `python -m unittest discover -s tests` fails with
"Start directory is not importable", which is the first command someone who
just cloned the repository is likely to run.
"""
